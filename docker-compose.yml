services:
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      torro:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      # Verify the full proxy path to the backend. localhost resolves to ::1 in this
      # image but nginx listens on IPv4, so use 127.0.0.1 over the HTTPS vhost.
      test: ["CMD", "wget", "-q", "--no-check-certificate", "--spider", "https://127.0.0.1:443/health"]
      interval: 10s
      timeout: 5s
      retries: 3

  torro:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - "3000"
    environment:
      - QBIT_URL=http://qbittorrent:8080
      - DOWNLOADS_PATH=/downloads
      - DATA_DIR=/data
      - RP_ID=localhost
      - RP_NAME=Torro
      - NODE_ENV=production
      - COOKIE_SECURE=false
    volumes:
      - downloads:/downloads
      - torro_data:/data
      - ./.env:/run/config/.env:ro
    depends_on:
      qbittorrent:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:3000/health"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s

  qbittorrent:
    image: linuxserver/qbittorrent:latest
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=UTC
      - WEBUI_PORT=8080
    volumes:
      - downloads:/downloads
      - qbit_config:/config
    ports:
      - "9090:8080"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  downloads:
  qbit_config:
  torro_data:
